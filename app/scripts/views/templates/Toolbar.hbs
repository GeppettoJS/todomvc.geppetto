<span id="todo-count">{{t "todo:toolbar.active" count=activeCount }}</span>
<ul id="filters"></ul>
<button id="clear-completed" {{#unless completedCount }}class="hidden"{{/unless}}>
    {{t "todo:toolbar.clearCompleted"}}
</button>
